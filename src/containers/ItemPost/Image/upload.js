import React from "React";
import { useState } from "React";

const upload = () => {
  const [images, setImages] = useState([]);
  const [imgaesPreview, setImagesPreview] = useState([]);

  return (
    


  );
};

export default upload;
